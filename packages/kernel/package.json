{
  "name": "@frank/kernel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "FRANK-§7.4 context-pack assembler. Builds a signed, hash-addressed, minimized manifest of what an agent is allowed to know for one assignment. Memory is recalled through @frank/memory and carried in a distinct lower-trust section (FRANK-§2.3). Integrity uses canonical JSON + HMAC signing via @frank/policy key infrastructure.",
  "exports": {
    ".": "./src/index.ts",
    "./harness-broker": "./src/harness-broker.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "echo 'lint: kernel (schema + dependency gates run at repo root)'"
  },
  "dependencies": {
    "@frank/contracts": "workspace:*",
    "@frank/memory": "workspace:*",
    "@frank/policy": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "22.10.2",
    "typescript": "5.7.2",
    "vitest": "2.1.8"
  },
  "frank": {
    "layer": "packages",
    "mayDependOn": ["@frank/contracts", "@frank/memory", "@frank/policy"]
  }
}
