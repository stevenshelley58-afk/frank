{
  "name": "@frank/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Frozen cross-cutting contracts. FRANK-§6. Nothing in this package may import from any other workspace package.",
  "exports": {
    ".": "./src/index.ts",
    "./schemas/*": "./schemas/*"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "echo 'lint: contracts (schema validation runs at repo root)'"
  },
  "frank": {
    "layer": "contracts",
    "mayDependOn": []
  }
}
