{
  "name": "@frank/identity",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "FRANK-§15.2 identity and FRANK-§2.2 roles. A pluggable IdentityProvider port plus the server-side authorization matrix. Holds no provider coupling: Authentik is one implementation, not this package's shape.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "echo 'lint: identity (schema + dependency gates run at repo root)'"
  },
  "dependencies": {
    "@frank/contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "22.10.2",
    "typescript": "5.7.2",
    "vitest": "2.1.8"
  },
  "frank": {
    "layer": "packages",
    "mayDependOn": ["@frank/contracts"]
  }
}
