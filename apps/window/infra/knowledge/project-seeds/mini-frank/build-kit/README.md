# Mini Frank build kit

This is the future home of tested reusable source, fixtures and licence notices.
No external component is copied here until its repository record is approved.
The current shadcn/ui record is deliberately a candidate, so this kit starts
empty rather than pretending untested source is safe.
