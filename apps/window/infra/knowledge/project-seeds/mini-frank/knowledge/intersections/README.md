# Intersections

Add a record here only when combining two or more layers produces a useful
finding that no layer states alone. It must carry the sources of every input
layer and its own freshness rule.
