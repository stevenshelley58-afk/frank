# FRANK-§0.2: a third-party package may not become a core dependency merely
# because an agent can install it. Exact versions only; no lockfile drift.
save-exact=true
engine-strict=true
resolution-mode=highest
