/** Complete Drizzle parity for hand-written migrations 0011–0013. */
import { sql } from 'drizzle-orm';
import { bigint, boolean, check, foreignKey, index, integer, jsonb, primaryKey, text, timestamp, uniqueIndex, uuid } from 'drizzle-orm/pg-core';
import { chatConversation, chatMessage } from './chat-shell.js';
import { room } from './room-mission.js';
import { domain } from './shared.js';

const time = (name: string) => timestamp(name, { withTimezone: true, mode: 'date' });
const object = (column: any) => sql`jsonb_typeof(${column}) = 'object' and ${column} <> '{}'::jsonb`;
const nonBlank = (...columns: any[]) => sql.join(columns.map((column) => sql`length(btrim(${column})) > 0`), sql` and `);

export const harnessConfigRevision = domain.table('harness_config_revision', {
  id: uuid('id').primaryKey(), cellId: text('cell_id').notNull(), harnessId: text('harness_id').notNull(), revision: integer('revision').notNull(), config: jsonb('config').notNull(), status: text('status').notNull(), createdAt: time('created_at').notNull().defaultNow(), createdBy: text('created_by').notNull(), rollbackOf: uuid('rollback_of'),
}, (t) => [
  uniqueIndex('harness_config_revision_uidx').on(t.cellId, t.harnessId, t.revision), uniqueIndex('harness_config_revision_id_cell_harness_uidx').on(t.id, t.cellId, t.harnessId), uniqueIndex('harness_config_revision_active_uidx').on(t.cellId, t.harnessId).where(sql`${t.status} = 'active'`),
  foreignKey({ name: 'harness_config_revision_rollback_fk', columns: [t.rollbackOf, t.cellId, t.harnessId], foreignColumns: [t.id, t.cellId, t.harnessId] }).onDelete('restrict'),
  check('harness_config_revision_positive', sql`${t.revision} >= 1`), check('harness_config_revision_config_object', object(t.config)), check('harness_config_revision_status', sql`${t.status} in ('draft','active','superseded','rolled_back')`), check('harness_config_revision_ids_not_blank', nonBlank(t.cellId, t.harnessId, t.createdBy)),
]);

export const roomRoutePolicy = domain.table('room_route_policy', {
  id: uuid('id').primaryKey(), cellId: text('cell_id').notNull(), subjectKind: text('subject_kind').notNull(), subjectId: text('subject_id').notNull(), roomId: uuid('room_id'), revision: integer('revision').notNull(), profile: text('profile').notNull(), aliases: jsonb('aliases').notNull(), shadowMode: boolean('shadow_mode').notNull().default(false), active: boolean('active').notNull().default(true), createdAt: time('created_at').notNull().defaultNow(),
}, (t) => [
  uniqueIndex('room_route_policy_revision_uidx').on(t.cellId, t.subjectKind, t.subjectId, t.revision), uniqueIndex('room_route_policy_active_uidx').on(t.cellId, t.subjectKind, t.subjectId).where(sql`${t.active}`), foreignKey({ name: 'room_route_policy_room_cell_fk', columns: [t.roomId, t.cellId], foreignColumns: [room.id, room.cellId] }).onDelete('restrict'), check('room_route_policy_subject_kind', sql`${t.subjectKind} in ('central','project','room')`), check('room_route_policy_subject_consistent', sql`(${t.subjectKind} = 'room') = (${t.roomId} is not null)`), check('room_route_policy_positive', sql`${t.revision} >= 1`), check('room_route_policy_aliases_object', object(t.aliases)), check('room_route_policy_ids_not_blank', nonBlank(t.cellId, t.subjectId, t.profile)),
]);

export const harnessSessionLineage = domain.table('harness_session_lineage', {
  id: uuid('id').primaryKey(), cellId: text('cell_id').notNull(), harnessId: text('harness_id').notNull(), parentSessionId: uuid('parent_session_id'), conversationId: uuid('conversation_id'), roomId: uuid('room_id'), turnId: uuid('turn_id'), externalSessionId: text('external_session_id'), checkpoint: jsonb('checkpoint'), createdAt: time('created_at').notNull().defaultNow(), updatedAt: time('updated_at').notNull().defaultNow(),
}, (t) => [
  uniqueIndex('harness_session_lineage_id_cell_harness_uidx').on(t.id, t.cellId, t.harnessId), foreignKey({ name: 'harness_session_lineage_parent_fk', columns: [t.parentSessionId, t.cellId, t.harnessId], foreignColumns: [t.id, t.cellId, t.harnessId] }).onDelete('restrict'), foreignKey({ name: 'harness_session_lineage_conversation_cell_fk', columns: [t.conversationId, t.cellId], foreignColumns: [chatConversation.id, chatConversation.cellId] }).onDelete('restrict'), foreignKey({ name: 'harness_session_lineage_room_cell_fk', columns: [t.roomId, t.cellId], foreignColumns: [room.id, room.cellId] }).onDelete('restrict'), foreignKey({ name: 'harness_session_lineage_turn_cell_fk', columns: [t.turnId, t.cellId], foreignColumns: [chatTurn.id, chatTurn.cellId] }).onDelete('restrict'), check('harness_session_lineage_checkpoint_object', sql`${t.checkpoint} is null or jsonb_typeof(${t.checkpoint}) = 'object'`), check('harness_session_lineage_scope_present', sql`${t.conversationId} is not null or ${t.roomId} is not null`), check('harness_session_lineage_ids_not_blank', sql`${nonBlank(t.cellId, t.harnessId)} and (${t.externalSessionId} is null or length(btrim(${t.externalSessionId})) > 0)`),
]);

export const modelSnapshot = domain.table('model_snapshot', { id: uuid('id').primaryKey(), cellId: text('cell_id').notNull(), modelAlias: text('model_alias').notNull(), provider: text('provider').notNull(), capabilities: jsonb('capabilities').notNull(), price: jsonb('price').notNull(), observedAt: time('observed_at').notNull().defaultNow() }, (t) => [check('model_snapshot_capabilities_object', object(t.capabilities)), check('model_snapshot_price_object', object(t.price)), check('model_snapshot_ids_not_blank', nonBlank(t.cellId, t.modelAlias, t.provider))]);

export const harnessHealthObservation = domain.table('harness_health_observation', { id: uuid('id').primaryKey(), cellId: text('cell_id').notNull(), harnessId: text('harness_id').notNull(), healthy: boolean('healthy').notNull(), failureClass: text('failure_class'), detail: text('detail'), latencyMs: integer('latency_ms'), expiresAt: time('expires_at').notNull(), observedAt: time('observed_at').notNull().defaultNow() }, (t) => [index('harness_health_ttl_idx').on(t.cellId, t.harnessId, t.expiresAt), check('harness_health_latency_nonnegative', sql`${t.latencyMs} is null or ${t.latencyMs} >= 0`), check('harness_health_ttl_strict', sql`${t.expiresAt} > ${t.observedAt} and ${t.expiresAt} <= ${t.observedAt} + interval '5 minutes'`), check('harness_health_ids_not_blank', sql`${nonBlank(t.cellId, t.harnessId)} and (${t.failureClass} is null or length(btrim(${t.failureClass})) > 0)`)]);

export const harnessActivationAudit = domain.table('harness_activation_audit', { id: uuid('id').primaryKey(), cellId: text('cell_id').notNull(), harnessId: text('harness_id').notNull(), revisionId: uuid('revision_id').notNull(), action: text('action').notNull(), createdAt: time('created_at').notNull().defaultNow(), createdBy: text('created_by').notNull() }, (t) => [foreignKey({ name: 'harness_activation_audit_revision_cell_harness_fk', columns: [t.revisionId, t.cellId, t.harnessId], foreignColumns: [harnessConfigRevision.id, harnessConfigRevision.cellId, harnessConfigRevision.harnessId] }).onDelete('restrict'), check('harness_activation_audit_action', sql`${t.action} in ('promote','rollback')`), check('harness_activation_audit_ids_not_blank', nonBlank(t.cellId, t.harnessId, t.createdBy))]);

export const chatTurn = domain.table('chat_turn', { id: uuid('id').primaryKey(), cellId: text('cell_id').notNull(), conversationId: uuid('conversation_id').notNull(), roomId: uuid('room_id'), userMessageId: uuid('user_message_id'), assistantMessageId: uuid('assistant_message_id'), idempotencyKey: text('idempotency_key').notNull(), requestHash: text('request_hash').notNull(), input: jsonb('input').notNull(), state: text('state').notNull(), cancelledAt: time('cancelled_at'), finishedAt: time('finished_at'), createdAt: time('created_at').notNull().defaultNow(), updatedAt: time('updated_at').notNull().defaultNow() }, (t) => [
  uniqueIndex('chat_turn_id_cell_uidx').on(t.id, t.cellId), uniqueIndex('chat_turn_idempotency_uidx').on(t.cellId, t.conversationId, t.idempotencyKey), foreignKey({ name: 'chat_turn_conversation_cell_fk', columns: [t.conversationId, t.cellId], foreignColumns: [chatConversation.id, chatConversation.cellId] }).onDelete('restrict'), foreignKey({ name: 'chat_turn_room_cell_fk', columns: [t.roomId, t.cellId], foreignColumns: [room.id, room.cellId] }).onDelete('restrict'), foreignKey({ name: 'chat_turn_user_message_cell_fk', columns: [t.userMessageId, t.cellId], foreignColumns: [chatMessage.id, chatMessage.cellId] }).onDelete('restrict'), foreignKey({ name: 'chat_turn_assistant_message_cell_fk', columns: [t.assistantMessageId, t.cellId], foreignColumns: [chatMessage.id, chatMessage.cellId] }).onDelete('restrict'), check('chat_turn_request_hash', sql`${t.requestHash} ~ '^[a-f0-9]{64}$'`), check('chat_turn_input_object', object(t.input)), check('chat_turn_state', sql`${t.state} in ('queued','running','completed','failed','cancelled')`), check('chat_turn_ids_not_blank', nonBlank(t.cellId, t.idempotencyKey)), check('chat_turn_terminal_finished_paired', sql`(${t.state} in ('completed','failed','cancelled')) = (${t.finishedAt} is not null)`), check('chat_turn_cancelled_state_paired', sql`(${t.state} = 'cancelled') = (${t.cancelledAt} is not null)`), check('chat_turn_finished_after_created', sql`${t.finishedAt} is null or ${t.finishedAt} >= ${t.createdAt}`),
]);

export const chatTurnEvent = domain.table('chat_turn_event', { turnId: uuid('turn_id').notNull(), cellId: text('cell_id').notNull(), cursor: integer('cursor').notNull(), kind: text('kind').notNull(), payload: jsonb('payload').notNull(), createdAt: time('created_at').notNull().defaultNow() }, (t) => [primaryKey({ columns: [t.turnId, t.cursor] }), foreignKey({ name: 'chat_turn_event_turn_cell_fk', columns: [t.turnId, t.cellId], foreignColumns: [chatTurn.id, chatTurn.cellId] }).onDelete('cascade'), check('chat_turn_event_cursor_nonnegative', sql`${t.cursor} >= 0`), check('chat_turn_event_payload_object', object(t.payload)), check('chat_turn_event_kind_not_blank', nonBlank(t.cellId, t.kind))]);
export const chatTurnReceipt = domain.table('chat_turn_receipt', { turnId: uuid('turn_id').primaryKey(), cellId: text('cell_id').notNull(), receipt: jsonb('receipt').notNull(), createdAt: time('created_at').notNull().defaultNow() }, (t) => [foreignKey({ name: 'chat_turn_receipt_turn_cell_fk', columns: [t.turnId, t.cellId], foreignColumns: [chatTurn.id, chatTurn.cellId] }).onDelete('cascade'), check('chat_turn_receipt_object', object(t.receipt)), check('chat_turn_receipt_cell_not_blank', nonBlank(t.cellId))]);
export const harnessFallbackAttempt = domain.table('harness_fallback_attempt', { id: uuid('id').primaryKey(), cellId: text('cell_id').notNull(), turnId: uuid('turn_id').notNull(), attempt: integer('attempt').notNull(), harnessId: text('harness_id').notNull(), upstream: text('upstream'), outcome: text('outcome').notNull(), createdAt: time('created_at').notNull().defaultNow() }, (t) => [uniqueIndex('harness_fallback_attempt_uidx').on(t.turnId, t.attempt), foreignKey({ name: 'harness_fallback_attempt_turn_cell_fk', columns: [t.turnId, t.cellId], foreignColumns: [chatTurn.id, chatTurn.cellId] }).onDelete('cascade'), check('harness_fallback_attempt_positive', sql`${t.attempt} >= 1`), check('harness_fallback_attempt_ids_not_blank', nonBlank(t.cellId, t.harnessId, t.outcome))]);
export const chatTurnCheckpoint = domain.table('chat_turn_checkpoint', { id: uuid('id').primaryKey(), cellId: text('cell_id').notNull(), turnId: uuid('turn_id').notNull(), cursor: integer('cursor').notNull(), checkpoint: jsonb('checkpoint').notNull(), createdAt: time('created_at').notNull().defaultNow() }, (t) => [uniqueIndex('chat_turn_checkpoint_uidx').on(t.turnId, t.cursor), foreignKey({ name: 'chat_turn_checkpoint_turn_cell_fk', columns: [t.turnId, t.cellId], foreignColumns: [chatTurn.id, chatTurn.cellId] }).onDelete('cascade'), check('chat_turn_checkpoint_cursor_nonnegative', sql`${t.cursor} >= 0`), check('chat_turn_checkpoint_object', object(t.checkpoint)), check('chat_turn_checkpoint_cell_not_blank', nonBlank(t.cellId))]);

export const harnessJob = domain.table('harness_job', { id: uuid('id').primaryKey(), cellId: text('cell_id').notNull(), ownerId: text('owner_id').notNull(), roomId: uuid('room_id'), idempotencyKey: text('idempotency_key').notNull(), requestHash: text('request_hash').notNull(), harness: text('harness').notNull(), taskType: text('task_type').notNull(), scope: jsonb('scope').notNull(), input: jsonb('input').notNull(), allowedTools: jsonb('allowed_tools').notNull(), egressProfile: text('egress_profile').notNull(), status: text('status').notNull(), cancelledAt: time('cancelled_at'), finishedAt: time('finished_at'), createdAt: time('created_at').notNull().defaultNow(), updatedAt: time('updated_at').notNull().defaultNow() }, (t) => [
  uniqueIndex('harness_job_id_cell_uidx').on(t.id, t.cellId), uniqueIndex('harness_job_idempotency_uidx').on(t.cellId, t.idempotencyKey), foreignKey({ name: 'harness_job_room_cell_fk', columns: [t.roomId, t.cellId], foreignColumns: [room.id, room.cellId] }).onDelete('restrict'), check('harness_job_request_hash', sql`${t.requestHash} ~ '^[a-f0-9]{64}$'`), check('harness_job_harness', sql`${t.harness} = 'hermes'`), check('harness_job_task_type', sql`${t.taskType} = 'browser-research'`), check('harness_job_scope_object', sql`${object(t.scope)} and ${t.scope}->>'cell_id' = ${t.cellId} and ${t.scope}->>'owner_id' = ${t.ownerId}`), check('harness_job_input_object', object(t.input)), check('harness_job_tools_array', sql`jsonb_typeof(${t.allowedTools}) = 'array' and jsonb_array_length(${t.allowedTools}) > 0`), check('harness_job_egress', sql`${t.egressProfile} in ('research-public','research-allowlist')`), check('harness_job_status', sql`${t.status} in ('queued','running','completed','failed','cancelled')`), check('harness_job_ids_not_blank', nonBlank(t.cellId, t.ownerId, t.idempotencyKey)), check('harness_job_terminal_finished_paired', sql`(${t.status} in ('completed','failed','cancelled')) = (${t.finishedAt} is not null)`), check('harness_job_cancelled_state_paired', sql`(${t.status} = 'cancelled') = (${t.cancelledAt} is not null)`), check('harness_job_finished_after_created', sql`${t.finishedAt} is null or ${t.finishedAt} >= ${t.createdAt}`),
]);
export const harnessJobEvent = domain.table('harness_job_event', { jobId: uuid('job_id').notNull(), cellId: text('cell_id').notNull(), cursor: integer('cursor').notNull(), kind: text('kind').notNull(), payload: jsonb('payload').notNull(), createdAt: time('created_at').notNull().defaultNow() }, (t) => [primaryKey({ columns: [t.jobId, t.cursor] }), foreignKey({ name: 'harness_job_event_job_cell_fk', columns: [t.jobId, t.cellId], foreignColumns: [harnessJob.id, harnessJob.cellId] }).onDelete('cascade'), check('harness_job_event_kind', sql`${t.kind} in ('progress','artifact','error','terminal')`), check('harness_job_event_cursor_nonnegative', sql`${t.cursor} >= 0`), check('harness_job_event_payload_object', object(t.payload)), check('harness_job_event_cell_not_blank', nonBlank(t.cellId))]);
export const harnessJobCancel = domain.table('harness_job_cancel', { jobId: uuid('job_id').primaryKey(), cellId: text('cell_id').notNull(), requestedBy: text('requested_by').notNull(), reason: text('reason'), createdAt: time('created_at').notNull().defaultNow() }, (t) => [foreignKey({ name: 'harness_job_cancel_job_cell_fk', columns: [t.jobId, t.cellId], foreignColumns: [harnessJob.id, harnessJob.cellId] }).onDelete('cascade'), check('harness_job_cancel_ids_not_blank', nonBlank(t.cellId, t.requestedBy))]);
export const harnessJobReceipt = domain.table('harness_job_receipt', { jobId: uuid('job_id').primaryKey(), cellId: text('cell_id').notNull(), receipt: jsonb('receipt').notNull(), createdAt: time('created_at').notNull().defaultNow() }, (t) => [foreignKey({ name: 'harness_job_receipt_job_cell_fk', columns: [t.jobId, t.cellId], foreignColumns: [harnessJob.id, harnessJob.cellId] }).onDelete('cascade'), check('harness_job_receipt_object', object(t.receipt)), check('harness_job_receipt_cell_not_blank', nonBlank(t.cellId))]);

export const objectManifest = domain.table('object_manifest', { objectId: uuid('object_id').primaryKey(), cellId: text('cell_id').notNull(), bucket: text('bucket').notNull().default('frank-objects'), objectKey: text('object_key').notNull(), sha256: text('sha256').notNull(), sizeBytes: bigint('size_bytes', { mode: 'bigint' }).notNull(), mediaType: text('media_type').notNull(), manifest: jsonb('manifest').notNull(), createdAt: time('created_at').notNull().defaultNow() }, (t) => [uniqueIndex('object_manifest_sha_uidx').on(t.cellId, t.sha256), uniqueIndex('object_manifest_id_cell_uidx').on(t.objectId, t.cellId), check('object_manifest_bucket', sql`${t.bucket} = 'frank-objects'`), check('object_manifest_sha256', sql`${t.sha256} ~ '^[a-f0-9]{64}$'`), check('object_manifest_size', sql`${t.sizeBytes} >= 0 and ${t.sizeBytes} <= 2147483648`), check('object_manifest_key_sha_match', sql`${t.objectKey} = 'sha256/' || substr(${t.sha256}, 1, 2) || '/' || ${t.sha256}`), check('object_manifest_payload_object', object(t.manifest)), check('object_manifest_ids_not_blank', nonBlank(t.cellId, t.mediaType))]);
export const uploadReservation = domain.table('upload_reservation', { id: uuid('id').primaryKey(), cellId: text('cell_id').notNull(), ownerId: text('owner_id').notNull(), conversationId: uuid('conversation_id').notNull(), draftMessageId: uuid('draft_message_id').notNull(), idempotencyKey: text('idempotency_key').notNull(), requestHash: text('request_hash').notNull(), uploadId: text('upload_id').notNull(), state: text('state').notNull(), reservedBytes: bigint('reserved_bytes', { mode: 'bigint' }).notNull(), reservedCount: integer('reserved_count').notNull().default(1), capabilityVersion: integer('capability_version').notNull().default(1), expiresAt: time('expires_at').notNull(), createdAt: time('created_at').notNull().defaultNow(), updatedAt: time('updated_at').notNull().defaultNow() }, (t) => [uniqueIndex('upload_reservation_id_cell_uidx').on(t.id, t.cellId), uniqueIndex('upload_reservation_upload_uidx').on(t.cellId, t.uploadId), uniqueIndex('upload_reservation_idempotency_uidx').on(t.cellId, t.ownerId, t.conversationId, t.draftMessageId, t.idempotencyKey), index('upload_reservation_expiry_idx').on(t.cellId, t.state, t.expiresAt), foreignKey({ name: 'upload_reservation_conversation_cell_fk', columns: [t.conversationId, t.cellId], foreignColumns: [chatConversation.id, chatConversation.cellId] }).onDelete('restrict'), check('upload_reservation_request_hash', sql`${t.requestHash} ~ '^[a-f0-9]{64}$'`), check('upload_reservation_state', sql`${t.state} in ('authorized','uploading','completed','cancelled','expired','rejected')`), check('upload_reservation_bytes', sql`${t.reservedBytes} >= 0 and ${t.reservedBytes} <= 2147483648`), check('upload_reservation_count', sql`${t.reservedCount} = 1`), check('upload_reservation_capability_version', sql`${t.capabilityVersion} >= 1`), check('upload_reservation_expiry_24h', sql`${t.expiresAt} > ${t.createdAt} and ${t.expiresAt} <= ${t.createdAt} + interval '24 hours'`), check('upload_reservation_ids_not_blank', nonBlank(t.cellId, t.ownerId, t.idempotencyKey, t.uploadId))]);
export const attachmentQuota = domain.table('attachment_quota', { cellId: text('cell_id').primaryKey(), pool: text('pool').notNull().default('global-cell'), reservedBytes: bigint('reserved_bytes', { mode: 'bigint' }).notNull().default(0n), updatedAt: time('updated_at').notNull().defaultNow() }, (t) => [check('attachment_quota_pool', sql`${t.pool} = 'global-cell'`), check('attachment_quota_bytes', sql`${t.reservedBytes} >= 0 and ${t.reservedBytes} <= 53687091200`), check('attachment_quota_cell_not_blank', nonBlank(t.cellId))]);
export const attachmentMessageQuota = domain.table('attachment_message_quota', { cellId: text('cell_id').notNull(), draftMessageId: uuid('draft_message_id').notNull(), reservedBytes: bigint('reserved_bytes', { mode: 'bigint' }).notNull().default(0n), reservedCount: integer('reserved_count').notNull().default(0), updatedAt: time('updated_at').notNull().defaultNow() }, (t) => [primaryKey({ columns: [t.cellId, t.draftMessageId] }), check('attachment_message_quota_bytes', sql`${t.reservedBytes} >= 0 and ${t.reservedBytes} <= 10737418240`), check('attachment_message_quota_count', sql`${t.reservedCount} >= 0 and ${t.reservedCount} <= 10000`), check('attachment_message_quota_cell_not_blank', nonBlank(t.cellId))]);
export const attachmentHostFreeObservation = domain.table('attachment_host_free_observation', { id: uuid('id').primaryKey(), hostId: text('host_id').notNull(), freeBytes: bigint('free_bytes', { mode: 'bigint' }).notNull(), usable: boolean('usable').notNull(), observedAt: time('observed_at').notNull().defaultNow(), expiresAt: time('expires_at').notNull() }, (t) => [index('attachment_host_free_observation_ttl_idx').on(t.hostId, t.expiresAt), check('attachment_host_free_observation_nonnegative', sql`${t.freeBytes} >= 0`), check('attachment_host_free_observation_usable_consistent', sql`${t.usable} = (${t.freeBytes} >= 32212254720)`), check('attachment_host_free_observation_ttl_strict', sql`${t.expiresAt} > ${t.observedAt} and ${t.expiresAt} <= ${t.observedAt} + interval '5 minutes'`), check('attachment_host_free_observation_host_not_blank', nonBlank(t.hostId))]);
export const attachment = domain.table('attachment', { id: uuid('id').primaryKey(), cellId: text('cell_id').notNull(), ownerId: text('owner_id').notNull(), conversationId: uuid('conversation_id'), draftMessageId: uuid('draft_message_id').notNull(), messageId: uuid('message_id'), turnId: uuid('turn_id'), reservationId: uuid('reservation_id').notNull(), uploadId: text('upload_id').notNull(), objectId: uuid('object_id'), name: text('name').notNull(), relativePath: text('relative_path'), sizeBytes: bigint('size_bytes', { mode: 'bigint' }).notNull(), mediaType: text('media_type').notNull(), digest: text('digest'), scanState: text('scan_state').notNull(), extractionState: text('extraction_state').notNull(), sourceRef: jsonb('source_ref').notNull(), state: text('state').notNull(), createdAt: time('created_at').notNull().defaultNow(), updatedAt: time('updated_at').notNull().defaultNow() }, (t) => [uniqueIndex('attachment_id_cell_uidx').on(t.id, t.cellId), uniqueIndex('attachment_upload_uidx').on(t.cellId, t.uploadId), index('attachment_owner_state_idx').on(t.cellId, t.ownerId, t.state, t.updatedAt), foreignKey({ name: 'attachment_conversation_cell_fk', columns: [t.conversationId, t.cellId], foreignColumns: [chatConversation.id, chatConversation.cellId] }).onDelete('restrict'), foreignKey({ name: 'attachment_message_cell_fk', columns: [t.messageId, t.cellId], foreignColumns: [chatMessage.id, chatMessage.cellId] }).onDelete('restrict'), foreignKey({ name: 'attachment_turn_cell_fk', columns: [t.turnId, t.cellId], foreignColumns: [chatTurn.id, chatTurn.cellId] }).onDelete('restrict'), foreignKey({ name: 'attachment_reservation_cell_fk', columns: [t.reservationId, t.cellId], foreignColumns: [uploadReservation.id, uploadReservation.cellId] }).onDelete('restrict'), foreignKey({ name: 'attachment_object_cell_fk', columns: [t.objectId, t.cellId], foreignColumns: [objectManifest.objectId, objectManifest.cellId] }).onDelete('restrict'), check('attachment_size', sql`${t.sizeBytes} >= 0 and ${t.sizeBytes} <= 2147483648`), check('attachment_digest', sql`${t.digest} is null or ${t.digest} ~ '^[a-f0-9]{64}$'`), check('attachment_scan_state', sql`${t.scanState} in ('pending','clean','blocked','failed')`), check('attachment_extraction_state', sql`${t.extractionState} in ('none','pending','complete','failed')`), check('attachment_state', sql`${t.state} in ('staging','scanning','ready','promoted','rejected','cancelled','expired')`), check('attachment_source_ref_object', sql`jsonb_typeof(${t.sourceRef}) = 'object' and ${t.sourceRef} ? 'kind' and ${t.sourceRef} ? 'id' and jsonb_typeof(${t.sourceRef}->'kind') = 'string' and jsonb_typeof(${t.sourceRef}->'id') = 'string' and length(btrim(${t.sourceRef}->>'kind')) > 0 and length(btrim(${t.sourceRef}->>'id')) > 0 and (not (${t.sourceRef} ? 'version') or (jsonb_typeof(${t.sourceRef}->'version') = 'string' and length(btrim(${t.sourceRef}->>'version')) > 0))), check('attachment_ids_not_blank', nonBlank(t.cellId, t.ownerId, t.uploadId, t.name, t.mediaType)), check('attachment_message_link_consistent', sql`${t.messageId} is null or ${t.messageId} = ${t.draftMessageId}`), check('attachment_state_consistent', sql`((${t.state} in ('staging','scanning')) and ${t.scanState} = 'pending' and ${t.objectId} is null) or ((${t.state} in ('ready','promoted')) and ${t.scanState} = 'clean' and ${t.objectId} is not null) or (${t.state} = 'rejected' and ${t.scanState} in ('blocked','failed')) or (${t.state} in ('cancelled','expired') and ${t.objectId} is null)`)]);
export const attachmentOutbox = domain.table('attachment_outbox', { id: uuid('id').primaryKey(), cellId: text('cell_id').notNull(), attachmentId: uuid('attachment_id').notNull(), kind: text('kind').notNull(), state: text('state').notNull().default('pending'), payload: jsonb('payload').notNull(), availableAt: time('available_at').notNull().defaultNow(), attempts: integer('attempts').notNull().default(0), completedAt: time('completed_at'), createdAt: time('created_at').notNull().defaultNow() }, (t) => [uniqueIndex('attachment_outbox_once_uidx').on(t.cellId, t.attachmentId, t.kind), foreignKey({ name: 'attachment_outbox_attachment_cell_fk', columns: [t.attachmentId, t.cellId], foreignColumns: [attachment.id, attachment.cellId] }).onDelete('cascade'), check('attachment_outbox_kind', sql`${t.kind} in ('hash_scan_promote','extract','cleanup','reconcile')`), check('attachment_outbox_state', sql`${t.state} in ('pending','leased','completed','failed','cancelled')`), check('attachment_outbox_payload_object', object(t.payload)), check('attachment_outbox_attempts', sql`${t.attempts} >= 0`), check('attachment_outbox_state_consistent', sql`(${t.state} = 'completed') = (${t.completedAt} is not null)`), check('attachment_outbox_cell_not_blank', nonBlank(t.cellId))]);
export const attachmentCleanupLedger = domain.table('attachment_cleanup_ledger', { id: uuid('id').primaryKey(), cellId: text('cell_id').notNull(), reservationId: uuid('reservation_id').notNull(), state: text('state').notNull(), attempts: integer('attempts').notNull().default(0), claimedAt: time('claimed_at'), completedAt: time('completed_at'), createdAt: time('created_at').notNull().defaultNow() }, (t) => [uniqueIndex('attachment_cleanup_once_uidx').on(t.cellId, t.reservationId), foreignKey({ name: 'attachment_cleanup_reservation_cell_fk', columns: [t.reservationId, t.cellId], foreignColumns: [uploadReservation.id, uploadReservation.cellId] }).onDelete('cascade'), check('attachment_cleanup_state', sql`${t.state} in ('pending','claimed','completed','failed')`), check('attachment_cleanup_attempts', sql`${t.attempts} >= 0`), check('attachment_cleanup_state_consistent', sql`(${t.state} = 'claimed') = (${t.claimedAt} is not null) and (${t.state} = 'completed') = (${t.completedAt} is not null)`), check('attachment_cleanup_cell_not_blank', nonBlank(t.cellId))]);
