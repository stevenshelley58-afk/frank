{
  "name": "@frank/adapter-tools-zapier-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Zapier MCP tool provider for FRANK. Discovers tools over the MCP Streamable-HTTP transport and exposes Gmail, Google Calendar, and Google Tasks as Frank tools behind the ToolProvider interface. Tools are proposals only — execution still goes through the action boundary (FRANK-§6.9).",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "echo 'lint: adapter-tools-zapier-mcp'"
  },
  "dependencies": {
    "@frank/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "5.7.2",
    "vitest": "2.1.8"
  },
  "frank": {
    "layer": "adapter",
    "mayDependOn": ["@frank/contracts"]
  }
}
