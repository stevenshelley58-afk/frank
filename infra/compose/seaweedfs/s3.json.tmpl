{
  "_comment": [
    "FRANK object storage — SeaweedFS S3 identity TEMPLATE.",
    "",
    "Spec: FRANK-§16.2 'Object storage | SeaweedFS S3 API, behind the ObjectStore contract |",
    "Immutable sources, artifacts and media'; FRANK-§6.13 (ObjectStore contract);",
    "FRANK-§15.7 'Object storage uses per-cell buckets and server-side encryption';",
    "FRANK-§17.2 'infrastructure code contains no secrets'.",
    "",
    "THIS FILE IS A TEMPLATE AND IS COMMITTED. The placeholders __S3_ACCESS_KEY__ and",
    "__S3_SECRET_KEY__ are substituted at container start from FRANK_S3_ACCESS_KEY and",
    "FRANK_S3_SECRET_KEY into /run/s3.json, which is a tmpfs mount. The rendered file never",
    "touches disk and never enters the image or the repository.",
    "",
    "Identities are per-workload, not per-human (FRANK-§15.2 'Workload identities for",
    "services; no shared human credentials'). Slice 1 defines one cell workload identity;",
    "per-module identities with narrower bucket prefixes arrive with the Capability Broker.",
    "",
    "The anonymous identity is deliberately absent: an anonymous identity with any action at",
    "all would make canonical sources readable without authentication, which FRANK-§15.6",
    "forbids."
  ],
  "identities": [
    {
      "name": "frank-cell",
      "credentials": [
        {
          "accessKey": "__S3_ACCESS_KEY__",
          "secretKey": "__S3_SECRET_KEY__"
        }
      ],
      "actions": [
        "Read",
        "Write",
        "List",
        "Tagging",
        "Admin"
      ]
    }
  ]
}
