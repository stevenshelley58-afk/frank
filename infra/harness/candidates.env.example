# Candidate slots are immutable OCI references. Leave unset until the candidate verifier
# records its registry digest, signature and SBOM evidence. `latest` is prohibited.
# LiteLLM 1.82.7 and 1.82.8 are expressly forbidden (2026 supply-chain incident).
FRANK_LITELLM_CANDIDATE_IMAGE=ghcr.io/berriai/litellm:1.96.0-stable@sha256:REPLACE_AFTER_COSIGN_VERIFY
FRANK_SEAWEEDFS_CANDIDATE_IMAGE=chrislusf/seaweedfs:4.29@sha256:REPLACE_AFTER_COSIGN_VERIFY
FRANK_TUSD_CANDIDATE_IMAGE=tusproject/tusd:v2.10.0@sha256:REPLACE_AFTER_COSIGN_VERIFY
FRANK_CLAMAV_CANDIDATE_IMAGE=clamav/clamav:1.5.3-debian@sha256:REPLACE_AFTER_COSIGN_VERIFY
FRANK_GOOSE_CANDIDATE_IMAGE=ghcr.io/block/goose:1.45.0@sha256:REPLACE_AFTER_COSIGN_VERIFY
FRANK_HERMES_CANDIDATE_IMAGE=ghcr.io/NousResearch/hermes:0.18.2@sha256:REPLACE_AFTER_COSIGN_VERIFY
FRANK_LETTA_CANDIDATE_IMAGE=letta/letta@sha256:REPLACE_AFTER_COSIGN_VERIFY

# Promotion copies candidate values to the separately protected current slot; rollback
# copies the previous current manifest to rollback. This file is never a deploy env file.
