# Reference only. This tunnel is remote-managed in Cloudflare Zero Trust.
# Configure these as Public Hostname routes in the Cloudflare dashboard.

routes:
  - hostname: hub.frank.fail
    service: http://localhost:3000
  - hostname: api.frank.fail
    service: http://localhost:8080
  - hostname: aionui.frank.fail
    service: http://localhost:3000
