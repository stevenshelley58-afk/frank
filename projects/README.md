# Projects

One folder per project Frank drives. Each project also has a **room** in the
chat (the doing-surface) and a **mini-Frank** (scoped hands); this directory is
the durable project workspace — its specs, notes, flow configs, and assets.

The Frank Console **Files** module browses this read-only, so anything dropped
here shows up automatically.
