# W1-1 — Delete Frank's harness layer

Task file for W1-1 (harness layer deletion). Status: complete — see final commit.

## HOT-FILE REQUEST 1
File: apps/api/src/main.ts
Change: Remove the harness imports + runner wiring (all references to W1-1-deleted modules):
- Remove line 43: `import { GooseAdapter, GooseAgentHarnessAdapter } from '@frank/adapter-harness-goose';`
- Remove line 44: `import { DurableChatTurnRunner } from './services/chat-turn-runner.js';`
- Remove line 45: `import { chatTurnRuntimeConfig } from './services/chat-turn-config.js';`
- Remove lines 94-106 (the whole chat runner block):
```
  const chatConfig = chatTurnRuntimeConfig(process.env);
  const chatTurnRunner = chatConfig ? new DurableChatTurnRunner({
    db: store.db,
    adapters: [new GooseAgentHarnessAdapter(new GooseAdapter({
      baseUrl: chatConfig.gooseAcpUrl.replace(/^ws/, 'http').replace(/\/acp\/?$/, ''),
      wsUrl: chatConfig.gooseAcpUrl,
      ...(chatConfig.gooseSecret ? { secretKey: chatConfig.gooseSecret } : {}),
    }))],
    modelAliases: chatConfig.aliases,
    capabilityRoutes: chatConfig.capabilityRoutes,
    workspacePath: chatConfig.workspacePath,
  }) : undefined;
  await chatTurnRunner?.recover();
```
- Remove line 231: `    ...(chatTurnRunner === undefined ? {} : { chatTurnRunner }),`
- Remove line 247: `      .then(async () => chatTurnRunner?.shutdown())`
Reason: DurableChatTurnRunner (services/chat-turn-runner.ts), chatTurnRuntimeConfig (services/chat-turn-config.ts), and @frank/adapter-harness-goose (adapters/harness/) were deleted by W1-1.

## HOT-FILE REQUEST 2
File: apps/api/src/server.ts
Change: Remove all harness-control route wiring:
- Remove line 85: `import { harnessControlRoutes, registerHarnessControlRoutes } from './routes/harness-control.js';`
- Remove line 114: `  ...harnessControlRoutes,`
- Line 243: remove `...harnessControlRoutes, ` from the set, leaving:
  `const databaseRoutes = new Set<AnyRouteDefinition>([frameGetRoute, ...chatTurnRoutes, ...attachmentRoutes, ...attachmentUploadRoutes]);`
- Remove line 494: `    registerHarnessControlRoutes(app, { ...shared, db: options.db });`
Reason: routes/harness-control.ts (+ test) deleted by W1-1. NOTE: the `chatTurnRunner` option (lines 84, 200, 327, 493) is typed by `ChatTurnRunner` from routes/chat-turns.ts, which REMAINS — do not remove it.

## HOT-FILE REQUEST 3
File: packages/contracts/src/index.ts
Change: Remove the two deleted module re-exports:
- Remove line 23: `export * from './harness.js';`
- Remove line 26: `export * from './harness-control.js';`
Reason: packages/contracts/src/harness.ts and harness-control.ts deleted by W1-1.

## HOT-FILE REQUEST 4
File: pnpm-lock.yaml
Change: Remove the `@frank/adapter-harness-goose` workspace package entry (its snapshots/importers references under `apps/api`). Regenerate via `pnpm install` at the wave gate.
Reason: adapters/harness/goose deleted by W1-1; apps/api/package.json no longer depends on it.

## HANDOFF NOTES (non-blocking)
- W1-3 scope: apps/api/src/services/workbench/** (harness-executor.ts, recipes/container-agent.ts, recipes/cli-headless.ts, recipes/goose-headless.ts, recipes/harness-swap.test.ts, recipes/hitl3-ask-policy.test.ts, container-executor.ts) still import `AgentHarnessAdapter`/`HarnessEvent` from @frank/contracts and `GooseAdapter`/`SessionHandle`/`StreamChunk` from @frank/adapter-harness-goose. These packages are deleted — those files are broken until W1-3 lands (next batch). Done-when greps for W1-1 clear fully after W1-3.
- Retained intentionally: routes/chat-turns.ts (defines ChatTurnRunner, reads harness_fallback_attempt rows for receipts); contracts/chat-turn.ts + evidence.ts `harness: string` data fields; adapters/storage/postgres schema harness_* tables + migration parity tests (migrations remain); apps/api/src/test/harness.ts + db-harness.ts (generic test helpers); apps/web harness/providers feature (its own /api/providers + /api/chat concept, no imports of deleted code); infra/harness/** (deployment tooling).
- Retained doc-comment mentions of the word "harness" (not code references): routes/health.ts:9 (FRANK-§3.8 quote), services/enrichment.ts:37, services/attachments/runtime.ts:23, server.ts:460, missions/orchestrator.ts:1190 (data field).
