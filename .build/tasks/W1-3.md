# W1-3 — Delete the mission and workbench runners

Task file for W1-3 (mission/workbench runner deletion). Status: complete — see commits 7360a29, 68c6826, e53986d, 58ef5a7, db3f9f6, d592ab7.

## HOT-FILE REQUEST 1
File: apps/api/src/main.ts
Change: Remove all workbench/mission runner wiring (services deleted by W1-3):
- Remove lines 32-38: the seven `./services/workbench/*` imports + the `./services/missions/index.js` import:
  - 32: `import { ContainerWorkbenchExecutor } from './services/workbench/container-executor.js';`
  - 33: `import { LocalDockerCli, WorkbenchProvisioner } from './services/workbench/provisioner.js';`
  - 34: `import { WorkbenchRunner } from './services/workbench/runner.js';`
  - 35: `import { WorkbenchStore } from './services/workbench/store.js';`
  - 36: `import { WorkbenchTerminalReporter } from './services/workbench/terminal-reporter.js';`
  - 37: `import { WorkbenchCancellationService } from './services/workbench/cancellation.js';`
  - 38: `import { MissionOrchestrator, MissionPlanner } from './services/missions/index.js';`
- Line 31 `import { CostRepository, decimal, money } from '@frank/adapter-postgres';` — CostRepository/decimal/money are used ONLY inside the runner block (lines 131-143); remove the whole import if no other usage remains.
- Remove lines 91-171 (the entire `const runnerEnabled = ...` block: workbenchRunner/workbenchStore/providerApiKey vars, WorkbenchStore/LocalDockerCli/WorkbenchProvisioner/ContainerWorkbenchExecutor construction, the recordUsage CostRepository callback, WorkbenchRunner construction + `await workbenchRunner.start()`).
- Remove lines 173-205 (the entire `const missionEnabled = ...` block: missionEnabled guard, missionOrchestrator var, MissionPlanner + MissionOrchestrator construction, `await missionOrchestrator.start()`).
- Remove lines 211-212 from the buildServer call:
  - 211: `...(workbenchRunner === undefined ? {} : { workbenchRunner }),`
  - 212: `...(missionOrchestrator === undefined ? {} : { missionOrchestrator }),`
- Remove lines 220-221 from the shutdown chain:
  - 220: `.then(async () => missionOrchestrator?.stopScheduler())`
  - 221: `.then(async () => workbenchRunner?.stop())`
- Remove lines 250-252 (`function appLoggerFallback` — only used by the deleted mission log callback).
Reason: services/workbench/** + services/missions/** deleted by W1-3 commit 7360a29.

## HOT-FILE REQUEST 2 (SUPERSEDED — see CONCURRENCY NOTE below)
File: apps/api/src/server.ts
Change: Remove workbench/mission wiring ONLY. Channels (CH-06) and folder-binding (FS-02) SURVIVE — their stores were RELOCATED to services/channels/channel-push.ts + services/folder-binding/folder-binding-store.ts by the coordinator (see CONCURRENCY NOTE); server.ts channel/folder-binding imports + registration must be re-pointed to the new paths, NOT deleted. NOTE: the coordinator is already applying this edit in the working tree (uncommitted) — do not double-apply.
- Remove imports: workbench routes (65), missions routes (66-70), workbench-events (72), services/workbench/* service imports (74-80, 90-92) — keep `channelRoutes`/`registerChannelRoutes` (71) and `folderBindingRoutes`/`registerFolderBindingRoutes` (73) but re-point `RoomFolderBindingStore` + `ChannelPushStore` imports to '../services/folder-binding/folder-binding-store.js' + '../services/channels/channel-push.js'.
- Remove from ALL_ROUTES: `...workbenchAllRoutes,` + `...missionRoutes,` (101-102). KEEP `...channelRoutes,` + `...folderBindingRoutes,` (103-104).
- Remove BuildServerOptions dead options: workbenchPollIntervalMs (193-194), previewDeployer (203), workbenchRunner (205), missionOrchestrator (207).
- Remove workbenchDecisions + stagedWrites construction (415-426), the onDecisionResolution option in registerWorkRoutes (433-479), the workbench registration block (493-545: workbenchBus/workbenchFrontDoor/workbenchCancellation/previewBackend/registerWorkbenchRoutes/registerWorkbenchEventsRoute — but KEEP `registerChannelRoutes` + `registerFolderBindingRoutes` calls with re-pointed store constructors), and registerMissionRoutes (548-554).
Reason: workbench/mission deleted by W1-3 commit 7360a29; channels/folder-binding relocated by coordinator (see CONCURRENCY NOTE).

## CONCURRENCY NOTE (read before applying hot-file requests 2 and 3)
An external actor (AG-0/coordinator) is concurrently RELOCATING the channel-push and folder-binding stores out of the deleted services/workbench dir instead of deleting the channels/folder-binding features:
- They created `apps/api/src/services/channels/channel-push.ts` + `apps/api/src/services/folder-binding/folder-binding-store.ts` (new home for the stores that W1-3's 7360a29 deleted as part of services/workbench/**).
- They restored `apps/api/src/routes/channels.ts`, `apps/api/src/routes/folder-binding.ts`, `apps/api/src/schema/folder-binding.ts`, `apps/api/src/test/channels-ch06.integration.test.ts`, `apps/api/src/test/folder-binding.integration.test.ts` with imports re-pointed at the new store paths.
- Their staged restorations were accidentally swept into W1-3 commit 2791650 (my final commit, message says "Files: .build/tasks/W1-3.md" but the index held their staged adds at commit time — channels.ts, folder-binding.ts, schema/folder-binding.ts, services/channels/channel-push.ts, services/folder-binding/folder-binding-store.ts, channels-ch06.integration.test.ts, folder-binding.integration.test.ts). The swept content is exactly what they staged, so nothing was lost; the commit message is simply incomplete.
- Their IN-FLIGHT, UNCOMMITTED working-tree edits remain: `apps/api/src/routes/channels.ts` (import re-point to services/channels) and `apps/api/src/server.ts` (wiring re-point + workbench/mission removal). DO NOT commit or modify these — they belong to the coordinator's in-flight work.
- W1-3's 7360a29 also deleted routes/channels.ts, routes/folder-binding.ts, schema/folder-binding.ts, channels-ch06 + folder-binding integration tests; the coordinator's relocation supersedes those deletions — treat channels/folder-binding as SURVIVING (only the workbench runner/mission system is gone).
- CAUTION: the two swept test files (apps/api/src/test/channels-ch06.integration.test.ts:18, folder-binding.integration.test.ts:20) were restored as-is and still import the OLD paths `../services/workbench/channel-push.js` + `../services/workbench/folder-binding-store.js` — the relocation must re-point these to `../services/channels/channel-push.js` + `../services/folder-binding/folder-binding-store.js` or they will not compile (services/workbench is deleted).
- LIVE TYPE ERROR in the coordinator's in-flight frame edit (verified 14:54, not W1-3's): `src/routes/frame.ts(103,51) TS2345` — the handler's `running` array mixes `{ kind: 'workbench' }` rows with `{ kind: 'chat' }` rows; the response schema's union wants chat-shaped members. frame.ts + schema/frame.ts are modified in the working tree by the coordinator right now (this is the same class as W1-4's receipt chat-narrowing fix). W1-3 did not touch frame.ts — attribute to the in-flight edit.

## HOT-FILE REQUEST 3 (SUPERSEDED — see CONCURRENCY NOTE above)
File: apps/api/src/test/api-contract.test.ts
Change: In the ADR-017 OpenAPI expected-paths list (sorted), remove ONLY the paths registered by deleted routes. KEEP channels + folder-binding paths (their routes survive, relocated):
- REMOVE: 828-831 (missions comment + '/v1/missions', '/v1/missions/{id}', '/v1/missions/{id}/stop'), 835-836 ('/v1/rooms/{roomId}/files'), 840-841 ('/v1/rooms/{roomId}/workbenches'), 851-865 (all '/v1/workbenches*').
- KEEP: 813-815 ('/v1/channels/outbox', '/v1/channels/outbox/ack'), 832-834 ('/v1/rooms/{roomId}/channel-bindings', +'/{id}'), 837-839 ('/v1/rooms/{roomId}/folder-bindings', +'/{id}').
Reason: routes/missions.ts + routes/workbench.ts deleted by W1-3 commit 7360a29; channels/folder-binding routes survive (relocated stores).

## HOT-FILE REQUEST 4
File: adapters/storage/postgres/migrations/meta/_journal.json
Change: Append the 0015 entry. File is CRLF. After the idx-14 line
`    { "idx": 14, "version": "7", "when": 1787300004000, "tag": "0014_brain_legacy_rename", "breakpoints": true }`
add a comma and the new entry:
`    { "idx": 15, "version": "7", "when": 1787300005000, "tag": "0015_legacy_runner_tables", "breakpoints": true }`
Reason: new migration file adapters/storage/postgres/migrations/0015_legacy_runner_tables.sql (guarded renames of mission/workbench/workbench_artifact/workbench_decision/workbench_event/workbench_plan_step/workbench_receipt → legacy_*; tables absent in prod — row counts n/a, verified 2026-08-13) committed in 58ef5a7. NOTE: guard uses `frank_domain.<name>` (where migrations 0004/0005/0009 create the tables; db.ts pins search_path=frank_domain,public), not the packet's `public.<name>` template which would never match.

## HOT-FILE REQUEST 5
File: apps/web/src/components/shell/frank-shell.tsx
Change: Remove workbench/mission shell wiring (web libs deleted by W1-3):
- Line 27: `import { stopMission } from '@/lib/missions/client';`
- Line 28: `import { WORKBENCH_API } from '@/lib/workbench/types';`
- Lines 301-313: `const stopWorkbench = async (id: string) => {...}` handler
- Lines 315-322: `const stopFrameMission = async (id: string) => {...}` handler
- Lines 619-620: `onStopWorkbench={(id) => void stopWorkbench(id)}` and `onStopMission={(id) => void stopFrameMission(id)}` props (verify the parent component accepts/requires them after removal).
Reason: apps/web/src/lib/workbench/** + apps/web/src/lib/missions/** deleted by W1-3 commit e53986d.

## HOT-FILE REQUEST 6 (non-blocking notes)
- docs/requirements/registry.json + registry.md — AG-0 regenerates at gate (do not hand-edit).
- routes/frame.ts (NOT coordinator-only, but NOT in W1-3 scope) still raw-SQLs `frank_domain.workbench`, `frank_domain.mission`, `frank_domain.workbench_receipt` (lines 128/138/158) — tables renamed to legacy_* by migration 0015 when present (absent in prod, so no-op there). Suggest a follow-up to trim frame.ts's workbench/mission projection or it will break on any DB where 0015 fires.
- apps/web/src/lib/delegation-store.ts still POSTs to `/v1/workbenches` (deleted route) — the delegations surface is now dead; follow-up task recommended (not in W1-3 scope).
- apps/web/src/lib/same-origin.test.ts uses `/api/missions` as example URLs — pure URL-string fixtures, not imports; left untouched.

## HANDOFF NOTES
- adapter run persistence: schema/run.ts + repositories/run.ts deleted (RunRepository consumed ONLY by the adapter's own run-state-machine.integration.test.ts — verified no apps/api consumer). run-state.ts (pure TS state machine) KEPT — schema.test.ts + run-state.test.ts (out of scope) import it. run.js exports removed from schema/index.ts + repositories/index.ts barrels.
- codegraph-production-inputs.test.ts: no workbench imports (only node:fs + vitest) — kept.
- api-contract.test.ts still lists channels/folder-binding/mission/workbench paths — see HOT-FILE REQUEST 3.
