# W2-1 — Hermes client (claimed 2026-08-13)

Status: ✅ COMPLETE · branch `rebuild/wave2` · commits 99e8658..efc4044

See `.build/tasks/W2-1_PACKET.md` for the full packet. This file is the claim +
hot-file landing pad.

## CONCURRENCY NOTE (2026-08-13 ~15:47–15:56)

A second writer (another agent instance, same task) was observed editing
`packages/hermes-client/src/index.ts`, `index.test.ts`,
`apps/api/src/routes/chat-turns.ts`, `apps/api/src/test/chat-turns.test.ts`,
and `apps/api/package.json` CONCURRENTLY with this agent (file mtimes
advancing under an active writer). Resolved as follows — cheap-agent is the
writer of record for the final state committed in this task:

- Transport: the committed `chat()` uses the **Responses API SSE** stream
  (`POST /v1/responses`, `stream: true`), verified live against the real
  Hermes gateway (tool-call events come through the same stream — W2-2 needs
  them). The parallel draft used chat/completions; both are permitted by the
  packet, Responses is preferred.
- chat-turns.ts: submit **streams the reply in the POST response** (SSE:
  turn/text/tool/done/error) and persists ONLY metadata. The parallel draft
  appended assistant reply text into `frank_domain.chat_turn_event` — that
  violates the packet's hard rule ("store only: turn id, profile, session
  key, status, started, finished. Never message text") and W2-2's
  "read back from Hermes, NOT from Frank's DB". Rejected.
- apps/api/package.json: the parallel draft's wiring (dependency +
  mayDependOn) matched this agent's exactly; kept as-is.
- If the parallel draft lands commits after this task's, AG-0 should compare
  and drop the later ones (this task's commits carry the verified state).

## HOT-FILE REQUEST 1
File: apps/api/src/server.ts
Change: Remove the Wave-1 runner plumbing from `registerChatTurnRoutes`
(the route no longer has a runner — chat turns stream from Hermes directly):
- Line 71: delete `import type { ChatTurnRunner } from './routes/chat-turns.js';`
- Line 177: delete `readonly chatTurnRunner?: ChatTurnRunner;`
- Line 178: delete `readonly chatTurnPollIntervalMs?: number;`
- Line 294: delete `if (options.chatTurnRunner) app.addHook('onClose', async () => options.chatTurnRunner?.shutdown());`
- Line 398: replace
  `registerChatTurnRoutes(app, { ...shared, db: options.db, ...(options.chatTurnRunner ? { runner: options.chatTurnRunner } : {}), ...(options.chatTurnPollIntervalMs === undefined ? {} : { pollIntervalMs: options.chatTurnPollIntervalMs }) });`
  with
  `registerChatTurnRoutes(app, { ...shared, db: options.db });`
Reason: W2-1 rewrote `apps/api/src/routes/chat-turns.ts` to call
`@frank/hermes-client`'s `chat()` and stream the result; the `ChatTurnRunner`
type and `runner`/`pollIntervalMs` deps are deleted with it. (Verify exact
line numbers — server.ts may have drifted.)

## HOT-FILE REQUEST 2
File: apps/api/src/test/harness.ts
Change: Remove the runner plumbing from `BuildTestServerOptions` and the
buildServer call:
- Line 88: delete `readonly chatTurnRunner?: import('../routes/chat-turns.js').ChatTurnRunner;`
- Line 89: delete `readonly chatTurnPollIntervalMs?: number;`
- Line 120: delete `...(options.chatTurnRunner === undefined ? {} : { chatTurnRunner: options.chatTurnRunner }),`
- Line 121: delete `...(options.chatTurnPollIntervalMs === undefined ? {} : { chatTurnPollIntervalMs: options.chatTurnPollIntervalMs }),`
Reason: same as HOT-FILE REQUEST 1 — the type import no longer exists in
`routes/chat-turns.ts`.

## HOT-FILE REQUEST 3
File: pnpm-lock.yaml
Change: Regenerate (pnpm install) — adds the `openai@7.4.0` dependency for
the new `packages/hermes-client` workspace package and the
`@frank/hermes-client` workspace link in `apps/api`.
Reason: new workspace package `packages/hermes-client` (TASK W2-1).

## HOT-FILE REQUEST 4 (non-blocking note)
- docs/requirements/registry.json + registry.md — AG-0 regenerates at gate
  (chatTurnSubmit's response changed from a JSON view to an SSE stream; the
  other three chat-turn operations are unchanged).
