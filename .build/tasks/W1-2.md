# W1-2 — Delete Frank's memory system

Task file for W1-2 (memory system deletion). Status: complete — see final commit b36efe5 / d5180cf.

## HOT-FILE REQUEST 1
File: apps/api/src/server.ts
Change: Remove all /v1/brain route wiring (routes/brain.ts deleted by W1-2):
- Remove line 81: `import { brainRoutes, registerBrainRoutes } from './routes/brain.js';`
- Remove line 111: `  ...brainRoutes,` (from the ALL_ROUTES array spread list)
- Remove line 491: `    registerBrainRoutes(app, { ...shared, db: options.db });`
Optional (stale comments, non-breaking): line 195 comment `/** Raw DB handle for brain routes (raw SQL queries, not yet in DomainStore). */` (the `db` option itself stays — chat/chat-turn/frame routes still use it) and line 501 comment `// so like brain it needs a DB handle.`
Reason: apps/api/src/routes/brain.ts (incl. brainRoutes + registerBrainRoutes exports) deleted by W1-2 commit d5180cf.

## HOT-FILE REQUEST 2
File: adapters/storage/postgres/migrations/meta/_journal.json
Change: Append the 0014 entry. File is CRLF. After the idx-13 line
`    { "idx": 13, "version": "7", "when": 1787300003000, "tag": "0013_attachment", "breakpoints": true }`
add a comma and the new entry:
`    { "idx": 14, "version": "7", "when": 1787300004000, "tag": "0014_brain_legacy_rename", "breakpoints": true }`
Reason: new migration file adapters/storage/postgres/migrations/0014_brain_legacy_rename.sql (renames brain_assertions/brain_entities/brain_links/brain_memories/brain_sources → legacy_brain_*; all 0 rows verified prod 2026-08-13) committed in b36efe5.

## HOT-FILE REQUEST 3
File: pnpm-lock.yaml
Change: Regenerate via `pnpm install` at the wave gate to drop the @frank/memory workspace package (its importer entries in apps/web + packages/kernel and its own snapshot).
Reason: packages/memory deleted (commit 61f457b); "@frank/memory": "workspace:*" removed from apps/web/package.json (62027ca) and packages/kernel/package.json (da34855); apps/web/next.config.mjs transpilePackages entry removed (d5180cf).

## HOT-FILE REQUEST 4
File: apps/api/src/test/api-contract.test.ts
Change: In the ADR-017 OpenAPI test (expected paths list, sorted), remove the two brain paths so the sorted-array assertion matches the route registry after brain.ts deletion:
- Remove `      '/v1/brain/save',`
- Remove `      '/v1/brain/search',`
(currently lines 812-813; the remaining entries keep their order).
Reason: routes/brain.ts deleted by W1-2; its paths no longer appear in the OpenAPI document once HOT-FILE REQUEST 1 is applied.

## HANDOFF NOTES (non-blocking)
- apps/web/src/app/console/memory/ (memory-console.tsx, page.tsx) + registry.ts `{ id: 'memory', ... icon: 'brain' }` entry still exist and fetch the now-deleted /api/memory — frontend cleanup is outside W1-2's listed scope (plan listed only apps/web/src/app/api/memory/). Recommend a follow-up task (or fold into a W1-4-style navigation sweep) to delete the console memory screen + registry entry + `'brain'` icon from the icon union.
- Comment-only mentions of the deleted system (no code impact, out of scope): apps/api/src/services/workbench/store.ts:12 ("like 0003's brain_entry"), apps/api/src/services/postgres-store.ts:124, apps/api/src/test/harness.ts:85, apps/api/src/routes/chats.ts:11, packages/contracts/src/module-manifest.ts:35 (doc example "brain.youtube"), adapters/storage/postgres/src/schema/conversation.ts:20 (FRANK-§20 quote), apps/api/src/services/workbench/store.ts.
- packages/identity/src/roles.ts still defines `brain.search.read` / `brain.entry.write` permission keys (+ identity.test.ts:90 uses 'brain.search.read'). Not @frank/memory code; out of W1-2 touch scope. Cross-task hit.
- brain_entry table (migration 0003) intentionally NOT renamed — W1-2 renames only the five untracked brain_* tables per plan; 0003 is a historical tracked migration.
- pnpm-lock.yaml retains 2 @frank/memory hits until AG-0 regenerates (HOT-FILE REQUEST 3).
- Kernel/web typecheck + kernel vitest currently blocked at packages/contracts/src/index.ts (missing ./harness.js + ./harness-control.js exports) — W1-1's deleted modules awaiting the coordinator's barrel fix (W1-1 HOT-FILE REQUEST 3). No W1-2 errors reported by tsc.
