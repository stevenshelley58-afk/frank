# Cleanup evidence - 2026-09-05

This is historical cleanup evidence for branch
`refactor/frank-first-principles-20260905`. The candidate and pending statements
below are not deployment records; the verified release summary is appended.

## Completed in the checkout

- Removed 268 plus 149 lines of dead modules and 143 lines of module-only
  tests (12 tests), including the obsolete workspace-foundation and
  unregistered work-tools paths.
- Removed the overwritten 16-line server initialization and changed the
  launcher from an unconsumed stdout pipe to `DEVNULL`, preventing the noisy
  128 KiB regression.
- Kept the active customer-operations role checks and published-provider
  readiness; only an unused static capability mirror was removed.
- Added a single Window verification runner. It isolates temporary data roots,
  runs Python tests, checks non-vendored JavaScript syntax, and runs
  non-browser JavaScript tests. CI invokes it once after
  `npm ci --ignore-scripts`.
- Minified bundle measurements: graph workbench raw
  12,232,201 to 5,570,436 bytes; dashboard raw 1,587,380 to 600,235 bytes.
- The graph browser suite passed 4/4 with zero skips. Baseline desktop/mobile
  browser evidence is at
  `/srv/frank/tmp/cleanup-20260905/baseline/browser.json`.
- Production Hub QA baseline observed two graph-bundle requests at startup,
  through the canonical URL and a cache-busted URL. The target is zero graph
  startup requests and one canonical lazy bundle identity when a graph is
  opened; final status awaits the frontend verification snapshot. These are raw
  bundle-size measurements, not transfer-size or speed claims.
- Current Ad Studio endpoints returned HTTP 200. The functioning surface remains
  in scope; older 404 probe evidence is historical and must not be treated as
  current status.

The cleanup baseline before these edits was 1,050 Python tests total, including
11 skipped, and 90 JavaScript tests passed. Full verification remains pending
the concurrent frontend routing and lifecycle repair and parent release checks.
No deployment is claimed by this document.

## Follow-up boundaries

- Lazy graph loading is active work in this cleanup batch; its final status
  belongs to the frontend verification result.
- A framework or GridStack rewrite is out of scope.
- Metadata editing, archive, and restore are implemented in the current source
  but are not released in this batch; this historical cleanup record is not
  release evidence for them.

## Working-tree verification snapshot

One `npm run verify` run completed on a concurrently changing candidate working
tree: 1,039 Python tests passed with 11 skipped, and 129 non-browser JavaScript
tests passed. The log is `/srv/frank/tmp/cleanup-20260905/verify-final.log`.
A candidate image build, `frank-window:cleanup-check`, also completed; an
isolated no-host-mount container imported `server` after creating only its
configured temporary data roots. Its record is
`/srv/frank/tmp/cleanup-20260905/container-build.log`.

These checks are a working-tree snapshot, not verification of an intended
release revision, because concurrent Mini files reappeared during the release
preparation window. Do not deploy from this evidence. Candidate browser QA
records zero graph-bundle requests at desktop and mobile startup, then one
canonical lazy request after opening a real pipeline graph. Evidence is
`navigation-qa-candidate.json`, `navigation-qa-pipeline-candidate.json`,
and `navigation-qa-pipeline-chunk-failure.json` under
`/srv/frank/tmp/cleanup-20260905/`. The deployed baseline had two startup
graph requests; the candidate result is not a production claim. Deployment,
post-deployment health checks, and real-browser verification of a committed
revision remain pending.

## Verified release summary

Production revision `4a0635964066b9eeb26c0df13dc56943a66b7068` was deployed through
PRs 135, 136, and 137. The verified production receipts are:

- `/srv/frank/tmp/cleanup-20260905/navigation-qa-production.json`
- `/srv/frank/tmp/cleanup-20260905/navigation-qa-pipeline-production.json`
- `/srv/frank/tmp/cleanup-20260905/browser-journey-production.json`
- `/srv/frank/tmp/cleanup-20260905/frank-health-production.json`

This supersedes the earlier candidate-only pending-deployment language. That release
reported a background full-collector warning.

The full-collector implementation has a host-read validation record at
`/srv/frank/tmp/cleanup-20260905/reconciliation-fix-check`: its full run
succeeded and correctly kept source revision `0dab8d5` separate from approved
revision `4a063596`. The root cause was that a valid approved pointer's
`revision_mismatch` value was discarded, causing full validation to
raise `ValueError`. This validates the source fix, not a production deployment. The release log
and live collector receipt must confirm the fix after deployment.
